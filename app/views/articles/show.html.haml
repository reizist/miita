%article.article
  .article-header
    .container
      %h1.article-title
        = @article.title
  .article-content
    .container
      = markdown(@article.content)

.article-version
  .container
    = render 'articles/versions'
